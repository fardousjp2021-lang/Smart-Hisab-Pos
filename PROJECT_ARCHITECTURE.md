# 📌 SMART HISAB POS — Master Project Blueprint & Architecture Specification

> **তারিখ**: ০৯ আগস্ট ২০২৬  
> **সুপার এডমিন ও ওনার**: ফারদৌস আল-আমিন (Fardous Alamin)  
> **প্ল্যাটফর্ম নাম**: **SmartHisab POS** (`SmartHisab POS — Cloud-Based Business POS & ERP Software`)  
> **প্রাথমিক ডিরেক্টরি**: `c:\Users\FARDOUS\Downloads\SMART HISAB`  
> **ভাষা**: সম্পূর্ণ বাংলা (Bengali UI & Communication)  

---

## 1. 🎯 প্রজেক্টের লক্ষ্য ও আইডেন্টিটি (Project Scope & Identity)

SmartHisab POS হলো একটি মাল্টি-টেন্যান্ট (Multi-Tenant) ক্লাউড-ভিত্তিক বিজনেস POS ও ERP সফটওয়্যার। এর সাহায্যে বাংলাদেশের যেকোনো ধরনের ব্যবসা প্রতিষ্ঠান (যেমন: মোবাইল ও ইলেকট্রনিক্স শপ, ফার্মেসি, মুদি দোকান/সুপারশপ, ক্লথিং/গার্মেন্টস ইত্যাদি) তাদের দৈনন্দিন হিসাব, পণ্য স্টক, মেমো প্রিন্টিং, কিস্তি ব্যবস্থাপনা এবং হিসাব-নিকাশ পরিচালনা করতে পারে।

---

## 2. 🛠️ টেকনিক্যাল স্ট্যাক ও ফ্রেমওয়ার্ক (Technical Stack)

- **ফ্রন্টএন্ড ফ্রেমওয়ার্ক**: React 18 (TypeScript - `.tsx`)
- **বিল্ড টুল**: Vite
- **স্টাইলিং**: Vanilla CSS / Tailwind CSS + Glassmorphism / Sleek Dark & Light Themes
- **আইকন সেট**: `lucide-react`
- **রুট এন্ট্রি ফাইল**: `index.html` ➔ `/src/main.tsx` ➔ `src/pos-app/App.tsx`
- **লোকাল সার্ভার পোর্ট**: `http://localhost:3000/`

---

## 3. 📂 প্রজেক্ট ফাইল ও উপাদান পরিচিতি (Project File Map)

| ফাইল / ডিরেক্টরি | কাজ ও বিবরণ |
| :--- | :--- |
| `index.html` | অ্যাপের মূল HTML টেমপ্লেট যা `<div id="root"></div>` এবং `/src/main.tsx` লোড করে। |
| `src/main.tsx` | React 18 `createRoot` এবং Error Boundary সহ মেইন রেন্ডারিং এন্ট্রি। |
| `src/pos-app/App.tsx` | সম্পূর্ণ অ্যাপের মেইন কন্ট্রোলার, রাউটিং, সাইডবার ও সুপার এডমিন ভিউ। |
| `src/pos-app/context/AppContext.tsx` | অ্যাপের গ্লোবাল স্টেট (CurrentUser, ShopName, System Employees, Login State)। |
| `src/pos-app/components/AdminPanel.tsx` | সুপার এডমিন ড্যাশবোর্ড (মার্চেন্ট তালিকা, সাবস্ক্রিপশন নবায়ন ও রিনিউয়াল)। |
| `src/pos-app/components/ImmersivePOS.tsx` | মেইন সেলস POS ইন্টারফেস (ক্যাশ রেজিস্টার, দ্রুত মেমো ও প্রিন্টিং)। |
| `src/pos-app/components/StoreSettings.tsx` | দোকান সেটিংস (দোকানের নাম, ঠিকানা, ভ্যাট/জিএসটি, ব্যাংক একাউন্ট ও মেমো নোটিশ)। *[Store Logo কার্ড রিমুভড]* |
| `src/pos-app/components/LandingPage.tsx` | ওয়েবসাইটের ল্যান্ডিং পেজ ও মার্চেন্ট লগইন মোডাল (পাসওয়ার্ড দেখা/লুকানোর আইকন সহ)। |
| `src/pos-app/components/StockCenter.tsx` | ইনভেন্টরি, প্রোডাক্ট স্টক এন্ট্রি, বারকোড এবং ক্যাটাগরি ম্যানেজমেন্ট। |
| `src/pos-app/components/WarrantySerialManager.tsx` | মোবাইল ও ইলেকট্রনিক্স দোকানের জন্য IMEI এবং সিরিয়াল নম্বর ওয়ারেন্টি ব্যবস্থাপনা। |
| `src/pos-app/components/BatchExpiryManager.tsx` | ফার্মেসির ওষুধের জন্য ব্যাচ নম্বর ও মেয়াদের তারিখ (Expiry Date) ট্র্যাকিং। |
| `src/pos-app/components/Installments.tsx` | পণ্য কিস্তিতে বিক্রির হিসাব (Installment Sales & Ledger)। |
| `src/pos-app/components/ExpenseCenter.tsx` | দোকানের দৈনন্দিন খরচ ও ক্যাশ ফ্লো ম্যানেজমেন্ট। |
| `src/pos-app/components/ReportCenter.tsx` | লাভ-ক্ষতি, বিক্রি ও অডিট রিপোর্ট সেন্টার। |

---

## 4. 🔑 সুপার এডমিন রুলস ও সাবস্ক্রিপশন সিকোয়েন্স (Super Admin Sequence Rules)

সুপার এডমিন প্যানেল (`AdminPanel.tsx` ও `App.tsx`)-এ মেম্বারশিপ বা সাবস্ক্রিপশন নবায়নের বাটনগুলোর ক্রমানুসারে তালিকা (Sequence) **সবসময় ঠিক নিচের মতো থাকতে হবে**:

1. `🎁 ৭ দিন` — (৭ দিনের ডেমো ট্রায়াল)
2. `🎁 ১৫ দিন` — (১৫ দিনের ডেমো ট্রায়াল)
3. `🚀 ১ মাস` — (১ মাসের সাবস্ক্রিপশন)
4. `⚡ ৬ মাস` — (৬ মাসের সাবস্ক্রিপশন)
5. `🌟 ১ বছর` — (১ বছরের সাবস্ক্রিপশন)
6. `⚙️ কাস্টম দিন` — (ইচ্ছামত দিন ইনপুট দেওয়ার পপআপ প্রম্পট)
7. `🚫 Block / 🔓 Unblock` — (দোকানের পোর্টালে সাময়িক ব্লক বা আনব্লক করা)

---

## 5. 🚪 সুপার এডমিন ১-ক্লিক পোর্টাল প্রবেশ (Super Admin Impersonation)

- **বাটন টেক্সট**: `🔑 দোকান পোর্টালে প্রবেশ (প্রোডাক্ট এন্ট্রি)`
- **কার্যকারিতা**: ক্লিক করার সাথে সাথে সুপার এডমিন যেকোনো মার্চেন্টের দোকানে প্রবেশ করবে (`setShopName(owner.shopName)`)।
- **ফেরত বাটন**: `↩️ সুপার এডমিন প্যানেলে ফিরে যান` (ক্লিক করলে পুনরায় সুপার এডমিন ড্যাশবোর্ডে ফিরবে)।

---

## 6. ⚙️ দোকান কাস্টমাইজেশন প্রোটোকল (Shop Customization Protocol)

প্রতিটি দোকানের চাহিদা অনুযায়ী সুপার এডমিন যেভাবে সেটিংস পরিবর্তন করবেন:
1. **দোকানের নাম ও যোগাযোগ**: `StoreSettings.tsx`-এ নাম, ঠিকানা ও ফোন নম্বর সেট করা।
2. **ভ্যাট ও ট্যাক্স সেটিংস**: ট্রেড লাইসেন্স নম্বর ও ট্যাক্স রেট কনফিগার করা।
3. **ব্যাংক ও পেমেন্ট কিউআর**: মেমোর ফুটারে প্রদর্শনের জন্য বিকাশ/নগদ রিসিভিং অপশন দিন।
4. **মেমো শর্তাবলী**: কাস্টম রসিদ শর্ত বা ওয়ারেন্টি রুলস লিখে দিন।
5. **স্টাফ পারমিশন**: কর্মচারীদের জন্য নির্দিষ্ট এক্সেস রোল সেটআপ করুন।

---

## 7. ⚠️ স্থায়ী নির্দেশাবলী (Permanent Agentic Guidelines)

1. **প্রজেক্ট আইডেন্টিটি পরিবর্তন নিষিদ্ধ**: সফটওয়্যারটির নাম সবসময় **SmartHisab POS**।
2. **এন্ট্রি পয়েন্ট অক্ষুণ্ণ রাখা**: `index.html` যেন সবসময় মূল রিয়্যাক্ট অ্যাপকে রেফার করে।
3. **ভাষার নিয়ম**: ব্যবহারকারীর সাথে সমস্ত কথা এবং UI এর টেক্সট প্রমিত ও সাবলীল **বাংলায়** লিখতে হবে।
4. **সার্ভার চেকিং**: সব কাজ শেষে লোকাল সার্ভার `http://localhost:3000/` সচল আছে কিনা তা নিশ্চিত হতে হবে।

====================================================================
